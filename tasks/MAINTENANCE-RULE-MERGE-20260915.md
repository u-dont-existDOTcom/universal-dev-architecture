# Merge only the owner-authorized maintenance scope rule

Owner instruction: "so merge the maintenance rule then unless there's a reason not to".

Scope: isolate the approved maintenance/admission instruction correction from the
unfinished browser creator draft. Direct owner-authorized maintenance needs no
model-routing receipt; autonomous delegated Work retains trusted evidence and
authentication. No runtime code, browser-launcher change, deployment, or unrelated
draft content is included. This is not permission for self-declared maintenance.

Verification: repository tests, deterministic audit, combined instruction-chain
regressions, source archive reconstruction and exact tree comparison. The existing
root-length advisory is unchanged; the combined-chain test passes. Hosted required
checks must pass before merge, with no administrative bypass.

Rollback: revert this isolated policy commit; no production data migration occurs.
This supersedes the prior branch-only maintenance-rule status after merge; the
browser creator draft remains separate and unmerged.
