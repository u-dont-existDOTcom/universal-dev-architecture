# Blocked Work -> source-bound Chat reasoning

Owner outcome: OPEN. Blocked execution must automatically obtain appropriate Chat reasoning and a reviewed same-task continuation without owner message relay. Extra High is the default; current source-bound supervisor escalation selects Pro. A task notification is not a reasoning decision, and an enqueued request is not delivery or successful recovery.

## Reproduced causes
The existing fleet classifier ignored internal structured blockers, treated missing progress as an owner/integrity problem, and could use a resolved or different-task blocker. Its routing helper emitted a legacy factual-only packet rather than the V6 request-bound decision route, with a tick-dependent request identity. The journal's local execution state was not connected to the live worker/watch registry.

## Child candidate
Reuse the existing file/Git source watcher, fleet supervisor, current V6 provider relay, central submission scheduler, strict decision admission, and native Work return path. No second scheduler, unrelated automation, inferred credential, or worker-authored semantic decision is introduced. Internal blockers and evidence gaps route to reasoning. True owner/external gates remain held. A read-only journal source adapter exports counts/stage/allowlisted blocker codes, never case IDs, journal passages, or arbitrary error messages.

Each review binds the current owner source/outcome, exact task, factual event boundary, and registered stable supervisor. Current supervisor-authorized Pro escalation is preserved. Pending and expired uncertain requests are not replayed. Typed route parsing prevents malformed lookalike messages from suppressing a genuine review. Watcher restart deduplication preserves recurring blockers as new observations when file provenance changes.

## Verification
The prior fleet consumer failed the motivating internal-blocker, missing-progress, obsolete-blocker and V6-routing checks. The candidate passed 46 affected tests covering fleet behavior, journal observation, actual V6 browser-relay parsing, native Work identity, same-thread continuation, and unknown-send fences. The final watcher recurrence/restart refinement passed 4 targeted tests. TypeScript no-emit checking passed. Test telemetry is retained by the canonical test-efficiency observer; no forced redundant green full/mutation run occurred.

## Live activation is not complete
No shared runtime, production database, live watch configuration, provider session, private journal state, model call, or publication target was changed. The current Claude integrator remains the sole shared-runtime owner. The deployment lock requires owner confirmation before live deploy/restart; this child does not supersede it. The original live journal submission remains completion-unknown and must not be replayed.

Next responsible actor: the active integrator, using INTEGRATOR-HANDOFF.md plus the private task-scoped activation packet. Required remaining evidence is one real blocked execution -> verified Chat decision -> authorized same-task continuation, or a truthful current human-only gate. Passing this child suite does not establish that live loop. Do not mark the owner outcome satisfied from this checkpoint.

Lesson closeout: apply the existing blocked-execution/Chat-reasoning and consumer-seam verification rules, not a new universal policy. The generating defects were missing source enrollment, a classifier fall-through and an obsolete route producer. A read-only check of the real journal state produces BLOCKED/COMPLETION_UNKNOWN with no private content. This confirms input compatibility only; live routing remains unproved.
