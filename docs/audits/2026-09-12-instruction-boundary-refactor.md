# Instruction-boundary refactor

Owner-authorized source candidate, not installed/live proof. Extends the existing Active Lesson Contract, preserves the validated final-output timestamp mechanism while applying the later per-turn correction, makes causal explanations evidence-bound, and consolidates only reviewed governance copies.

The current pre-change three-file Mission Control chain is 30,383 bytes, already below 32 KiB; the older packet's overflow finding is superseded by newer source. Local app AGENTS and Mission Control runtime code/archive are unchanged by this refactor. A separately observed three-file archive/source difference remains a packaging verification issue, not proof of deployed drift.

Public AskRigor protocols remain standalone. The public protocol migration is a separate candidate, not an import of this development governance.
