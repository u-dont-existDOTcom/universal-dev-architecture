# Permanent capability-challenge rotation

Status: HANDED_OFF_NOT_LIVE_VERIFIED — exclusive PRIMARY recovery control transferred to owner-authorized Campaign148 in task `01a09d40-823f-74f3-8a1e-5fe8ede3ed8f` at approximately01:00UTC on2026-09-14. This task and its subagents are quiescent; do not run competing PRIMARY/browser/provider operations. The last own observation was all three relevant containers stopped after the null-to-false Docker guard mismatch; Campaign148 is restoring the exact compatible fallback. Full live acceptance remains incomplete. Same lease deadline2026-09-14T20:45:11.808Z, epoch3 and both60000ms floors remain fixed. Original owner authority and all retained implementation remain below.

Quiescent handoff artifact: `outputs/Capability-rotation-quiescent-handoff-20260914.md` in the task workspace. It records exact container IDs, protected artifact locations, skipped filesystem-source checks, original unconsumed permit, reviewed helper/test hashes, and the unsent GitHub-only diagnostic inline pill still present in the exact owned bootstrap composer. `--resume-existing` was requested but stopped before any implementation; it does not exist. No new branch, merge, provider message, nonce publication, Somatic change or SECONDARY change occurred in this continuation. Do not mistake this handoff for completion.

## Current recovery authorization and execution

Owner wording: `do whatever you need to do let's get it working please`, answering the explicit question: `May I implement an audited recovery for that exact failed reservation and repair the app-selection path, without clearing history or bypassing security gates?` Source: current owner Work message; capture/check time above is not source-sent time. This authorizes only evidence-preserving recovery of the identified pre-composition test reservation, repair of exact per-message app selection and pre-click error provenance, and canonical live acceptance under the retained architecture. It does not authorize a general ambiguity reset, direct database edits, fabricated proofs, another lease extension, new epoch, ordinary work, SECONDARY/Somatic changes, credential changes or merge.

Execution: inspect exact retained failure/ledger identity; implement append-only single-writer recovery with exact evidence and authorization binding plus negative tests; inspect and repair only the owned PRIMARY app-selection controls; obtain independent exact-diff security review; deploy the compatible hardened build with rollback copies; verify canonical preflight and fresh current-challenge capability receipt and restored scheduler readiness. Persist failures and truthful send counts/timestamps. A healthy app, green tests, or cleared local status alone do not satisfy acceptance.

2026-09-14 00:36 UTC checkpoint: recovery source is retained in same-branch commits `3f8c6e657d57730007a1edc741831dd4a2427502` (passive compatibility) and `5474ef3442a3ae177d586888abbabf724747cc6b` (active recovery/preparation). Rebuilt active image `sha256:f27a665ceebf92ea54c6cc783acd5201e39b906a7c43f550c014b18d26c6e497` passed the exact-image full suite 405/405, typecheck and build. Rebuilt passive image `sha256:ab3e8bacbe0ca9dd651a57a2dc5c9150901f282e7f57fd2080b70e9be05cca29` passed the complete compatibility probe for both capability paths. These supersede the earlier unreferenced image IDs, which disappeared for an unconfirmed reason. Both rebuilt images are retained by stopped, network-disabled, no-op containers with no production mounts.

The root-only operator helper's independent static review closed with no blocking findings at SHA-256 `0ee033e7872df405469c0971f629a2037b788c89f1ce6a1a1f359a464a91726e`; five actual-function containment regressions pass. Protected evidence capture SHA-256 is `5355a11c6e8a62d347d816ee05a71eac87d060adc38b606cb413fe864bbd1e3c`. All invocation attempts so far were read-only preflight and failed before writer stop or evidence capture. No recovery permit has been consumed, no old history replaced, and no provider boundary or nonce publication occurred in this continuation.

Operational discriminator: the unchanged accepted `acab661` container became Docker-unhealthy with a newly observed start time of 00:18:30 UTC; this task did not restart it and the cause is not established. Both listeners remain loopback-only. Direct full daemon health at 00:33:35 returned HTTP 200 in 1844 ms: complete event chain and submission ledger valid, ACTIVE_LEASE, three current rotations. Runtime-status returned HTTP 200 in 6292 ms, exceeding the existing Docker healthcheck's 5000 ms timeout. The healthcheck duplicates full health work and requests a full snapshot. Intermittent response delays, not demonstrated database corruption, are the current diagnostic frontier. Preserve every semantic health/security assertion; investigate a narrowly bounded monitor-budget correction before rollout, not a reset or waiver of failed validation.

UI read-only evidence: the new Plugins picker contains GitHub but no exact Mission Control result. Developer mode appears enabled in its exact Security and login settings group. A tested DOM-observation module exists but is not yet integrated or deployed; no app-selection success or missing-connection cause is claimed. All diagnostics remain on the existing automation-owned PRIMARY page, with no provider Enter/send and no owner-workstation browser or clipboard action.

00:58 UTC safety checkpoint: unchanged full monitor exit0 in14.739s established the timeout-budget mismatch. Reviewed helper `b1d22f93c123a1f39ee5eb43526cb20b82e1ba42448f5fd0649d8ab4f70106f3` passed read-only preflight and14/14 operator regressions. Actual handoff then failed before candidate startup: Docker materialized `HostConfig.OomKillDisable=false` instead of the supplied `null`; full Config/Env and all other host/security fields matched. The active candidate and compatible fallback are both stopped/never-started; old runtime is quarantined with restart disabled. Evidence and consistent database backups were captured; original permit remains unconsumed. Dashboard is temporarily offline while the exact-existing-container continuation is implemented/reviewed. Do not rerun the one-shot deployment, delete evidence, remint/extend the permit, restore historical database bytes or restart the old incompatible image. Resume only with complete current-state equality, source-byte checks skipped by the early guard, exact identity/config checks and compatible fallback containment.

23:53 UTC implementation checkpoint: protected startup-only, exact-state pre-composition recovery is implemented with one `BEGIN IMMEDIATE` transaction validating the complete retained ledger and committing an explicit `RECOVERED_BEFORE_COMPOSITION` record. Collector recovery remains unavailable; the old admission is retained, not relabeled as a normal abort. App selection now runs in pre-submit preparation for all relay paths so future preparation failures can record their real `BEFORE_SUBMIT` abort. Affected recovery/runtime/routes/fencing tests pass 60/60; affected relay/preparation tests pass 61/61; typecheck passes. Production remains unchanged and zero provider messages/new boundaries occurred during this continuation.

Independent review identified a rollback compatibility hazard: the former live parser cannot read the new recovery state. Same-branch passive compatibility commit `3f8c6e657d57730007a1edc741831dd4a2427502` changes only the neutral authority module/declaration; its old runtime has no startup permit reader or recovery transaction hook. Exact passive image `sha256:c28f7cf6a6a4af384adcc71c019e7d59331f10a19b7448f81d1dc14cd5b9e249` passed its network-disabled synthetic compatibility probe for both capability paths: valid permits cannot clear ambiguity, recovered fixtures survive startup/fresh admission/ordinary writes, complete ledger prefixes and lease/boundary values are preserved. After recovery could have been consumed, rollback must use this compatible image and retained current database, never an older parser or a restored historical database. This is rollback evidence, not live recovery or receipt acceptance.

UI diagnosis remains in progress: the current plugin menu uses generic `data-fill`/`tabindex` rows, plugin wrapper attributes, and exact title spans followed by descriptions, rather than the former menu-option roles. GitHub is present in the initial menu; Mission Control has not yet been found. All inspection is confined to the exact automation-owned PRIMARY bootstrap page. Temporary search text is removed only when it exactly matches this probe's text; no Enter/send, transcript inspection, owner workstation browser/clipboard action, or provider message is used for diagnosis.

Active enforcement: exact ownership/empty-idle page before control inspection; no private locator or credential output; no recovery without retained positive pre-composition evidence and absent boundary; no mutation of historical ledger; ordinary relay cannot self-recover ambiguity; fixed lease/epoch/pacing/nonce/receipt guards remain mandatory. Current source bootstrap refreshed from live default branch 5cd536fec0b8f367998657394bde5eab81e53efa; existing scoped owner-outcome, activation and test-efficiency controls retained. Iteration uses affected tests; production rollout requires compatible exact-image gates and independent review.

Lease-renewal approval captured at 2026-09-13T20:18:50Z: `yes and why does it have only a 24hr lease? taht seems like another gate of extra work`, replying to the exact same-lease/24-hour renewal question. Change expiresAt only; no host, epoch, lease ID, issued-at, ownership or takeover change. Explain that deployment duration separately from capability-challenge TTL; do not silently introduce an automatic renewal controller.

## Historical pre-renewal checkpoint — superseded, do not repeat these actions

The following pre-renewal observations and requests are historical only. The approved renewal has since succeeded; preserve its fixed 2026-09-14T20:45:11.808Z deadline. They do not request or authorize another renewal.

Owner approval captured at 2026-09-13T19:38:06Z: `yes`, responding to the exact question about raising both PRIMARY pacing minimums from 20 to 60 seconds. This authorizes only the two named pacing settings. Source-sent timestamp is unavailable; the timestamp is Work capture/check time. Fresh read-only PRIMARY health reports LEASE_STALE; do not bypass or silently extend its expiry gate.

19:43 UTC continuation: the app's private configuration file was already 60000 ms although its running container retained 20000 ms. The relay file was changed from 20000 to 60000, with every unrelated byte and original ownership/mode verified unchanged. A root-only rollback copy was captured on PRIMARY. No service was restarted. Login remains reachable and unauthenticated API access remains 401; chain and ledger are valid, but send authority is blocked by lease expiry at 2026-09-13T16:21:43.570597Z.

Read-only code and service checks found no active renewal service or relay-facing renewal operation. Existing same-lease renewal is an operator configuration/startup transaction and can extend only expiresAt while preserving lease ID, epoch 3, PRIMARY identity, issued-at and takeover evidence. Ask the owner for a bounded 24-hour same-lease renewal; do not substitute a new epoch, change SECONDARY, weaken health/expiry validation, or restart with the known-invalid lease. Both sending flags remain disabled and this continuation sent zero provider messages or nonce comments. Retained branch and existing implementation remain unchanged apart from requirement records and the approved relay configuration value.

## Controlling architecture

Controlling architecture: `docs/requirements/MISSION-CONTROL-PERMANENT-CAPABILITY-ROTATION-DIRECTIVE-20260913.md`, preserved byte-for-byte with SHA-256 `86d900d9b2e433460e4a2f1c74b25c59a2ae44f4e5d224cb36b0d8b9f0207ccf`.

The owner's direct instruction to retain the current branch overrides the attachment's preferred branch name. Work continues on `task/mission-control-current-work-observability-20260913`. Earlier observability drafts remain retained and are not part of this capability-rotation deployment.

PRIMARY identity was directly verified: existing `mission-control-issue90-live`, loopback BFF/daemon, existing persistent single-writer SQLite volume. At the initial inspection there was no static receipt-policy environment value; materialize only the owner-approved repository, issues 59/60/61, writer `u-dont-existDOTcom`, and an initially empty legacy challenge list. The subsequent competing deployment introduced a policy, reconciled as documented below. No earlier historical policy is inferred.

## Enforcement / activated lessons

21:20 UTC retained checkpoint: exact app `acab661` is deployed and healthy; isolated publisher active, original hardening and exact approved relay-lock repair restored. Three automatic successors and exact issue-60 comments independently verified; actual daemon restart reused all three, retained all48 historical rows/hash and schema3; same live app reconnected after publisher restart. Final exact-image full suite388/388 passed after correcting diagnostic-only Git/state-layout/concurrency assumptions; source safeguards unchanged. Full privacy-safe evidence: `docs/evidence/2026-09-13-permanent-capability-rotation-primary.md`.

Live acceptance is BLOCKED, not complete. Initial model-menu readiness failed; one guarded reload of the same signed-in, idle, empty owned page restored fixed-control verification. Canonical preflight then failed exact Mission Control per-message app selection before message composition (21:16:52.432 UTC). That error lost its specific pre-click stage; no durable abort was recorded. A later canonical capability attempt marked the expired reservation AMBIGUOUS_AFTER_RESTART. Zero provider messages/new boundaries; fresh smoke mode-switching evidence exists, but no current MC/GitHub capability receipt. Current lease remains ACTIVE_LEASE, chain/ledger valid, send readiness false, queue depth0, unresolved ambiguous test admission. This run caused that unresolved test reservation and must not hide it behind app health. Existing abort/outcome operations cannot clear its already-ambiguous state. No direct DB edit, fabricated proof, epoch change, security weakening or replay is authorized. Stop for explicit narrowly audited recovery/app-selection authority; preserve current branch, runtime, publisher, credentials, history and gates. No further browser/provider operation is scheduled by this task.

20:49 UTC guarded rollout: the competing task confirmed its real protected Docker env-file source under `/etc/mission-control/issue90-caprot-9ee081a01362132bd728d77f8e5332c9de1c1c42.env`; the earlier home-directory env file is not the current container source and was left untouched. Exact private/live equality was proved without values in output. Same-lease expiry is now `2026-09-14T20:45:11.808Z`, epoch/identity unchanged. New app failed `CAPABILITY_PUBLIC_ALIAS_INVALID`; guarded fallback is healthy with original hardening restored, daemon writer PAT omitted, renewed expiry and DB history preserved. Forty-eight prior challenge rows were captured in a consistent root-private backup. All three current stable public aliases contain namespace colons (length 48); independent unauthenticated GitHub comment reads verified each exact alias was already published on issue 60 by the approved writer. Correct only the over-narrow new alias validator to accept bounded, nonempty colon-separated identifier segments; keep URLs/slashes, malformed segments, numeric-leading identifiers and excessive lengths rejected. Do not rewrite private registrations. Rebuild app and isolated broker together, then continue the same rollout. No provider message or new fixture was emitted by this failed app startup.

20:32 UTC reconciliation checkpoint: the pre-stop guard detected a competing deployment from task “Continue issue #90 implementation”, source `9ee081a01362132bd728d77f8e5332c9de1c1c42` / PR #108. That task explicitly confirmed quiescence and handed PRIMARY reconciliation to this task and its later owner-adopted publisher/socket architecture. It reports three issue-60 nonce fixtures and zero provider sends, no canonical preflight/capability acceptance. Retain its fixtures and additive SQLite `capability_challenges` history as historical, not current authority. The current app contains a newly introduced receipt policy and daemon writer-token delivery; these were absent at this task's earlier inspection. Compose the approved static channels, remove only that superseded daemon credential delivery (no token rotation/readback), and restore the isolated publisher service. Its container recreation dropped five prior hardening settings; restore the exact protected baseline while preserving every unrelated current setting. Renew only the same epoch-3 PRIMARY lease expiry per the owner approval above. Restore the exact approved relay-lock repair plus this branch's discovery delta. Do not touch the other task's source branch, SECONDARY or Somatic.

The original EventStore initialization unconditionally reset `user_version` to 2. Additive reconciliation now preserves any higher schema version and untouched retained tables/triggers. A focused restart/history regression must pass before creating the new exact app build. This migration does not import historical capability PASS into the new current-challenge authority.

| Trigger | Enforcement | Failure prevented |
| --- | --- | --- |
| Expiry / missing challenge | Durable candidate, exact publication verification, transactional current pointer | Double-active or unpublished authority |
| Restart or ambiguous network | Reuse immutable candidate; publication lookup before retry; fail closed on uncertainty | Duplicate issuance or false publication proof |
| New challenge | Exact current challenge in receipt and decision gates | Old PASS silently carries forward |
| Public nonce fixture | Four-field allowlist and fixed bus writer interface | MC nonce, locators or broad GitHub authority leak |
| Legacy config | One-way migration; dynamic state outranks old configured ID | Manual ID edits remain necessary |
| Live deployment | Preserve private config/queue/lease/pacing/browser and installed lock correction, reversible rollout | Unrelated runtime or credential change |
| Completion | Requirement matrix + live evidence, independent review | Unit tests substitute for owner outcome |

## Work sequence

1. Preserve directive and requirement; runtime-admit this bounded owner-direct implementation.
2. Add daemon-owned durable lifecycle and fixed-bus publication interface, plus expiry/concurrency/crash tests.
3. Integrate effective current policy through exact public challenge lookup, receipt reconciliation and decision admission. Add authenticated pair-specific relay discovery; keep old IDs migration-only.
4. Independent security review; focused/affected tests then applicable full release gates.
5. Discover/provision only an already-authorized narrow publisher path. Never copy broad account credentials to PRIMARY. If no compliant path exists, report that boundary without activating unpublished candidates.
6. Reversible exact-build PRIMARY rollout, genuine successor/publication/discovery/receipt acceptance. No Somatic, SECONDARY activation, merge, or ordinary provider work.

Test telemetry: `mc-capability-rotation-20260913`. Iteration lane until release verification.

## 03:44 UTC retained checkpoint (historical; superseded by continuation above)

Daemon-owned immutable candidates, lifecycle, atomic activation, 24-hour TTL / 4-hour renewal, effective-policy lookup and exact relay discovery are implemented. Rotation never grants PASS. Static receipt channels are materialized from the owner directive, not a claimed historical runtime value. Legacy IDs cannot outrank durable state, including after accidental feature disablement.

The narrow broker permits only issue-60 nonce-comment creation; no credential is passed to the daemon or relay. Owner provisioning source is `/etc/mission-control/capability-nonce-publisher/github-token` (root:root 0600; parent 0700). The unit provides `MISSION_CONTROL_CAPABILITY_GITHUB_TOKEN_FILE` as a path to its private systemd copy. Never ask for the PAT in chat or commands.

Independent review corrections: reserve canonical challenge/tool proof for the receipt ingester; require authenticated producer and exact worker binding for tool/mode consumption; derive mode producers from the existing relay registry; reject static fallback; limit publication search to the admissible lifetime window; mount a stable socket parent; enforce effective dump protection before credential access. No blocking source findings remained after the narrow final review. Source review is not live acceptance.

PRIMARY publisher is enabled/active after Joel manually provisioned its protected source credential. Writer authentication succeeded; no credential value was returned to Work. The same network-disabled diagnostic container reconnected after publisher restart through the stable parent mount, using only a rejected invalid request and zero GitHub mutations. Node v22.23.2, effective dump protection, and exact service-private credential-copy compatibility remain verified; no diagnostic override remains.

The exact d683877 app image built successfully on PRIMARY's pinned Node 22 runtime. Reversible rollout retained the old container/image, private configuration and consistent stopped-writer volume backup in a root-only PRIMARY rollback directory. Startup rejected the existing 20000 ms global pacing value because the approved source enforces 60000–600000 ms. The guard restored the original app and exact private configuration automatically. No pacing value or unrelated credential changed. Database history was retained rather than replaced; schema initialization may have occurred before the startup error, but challenge reconciliation was not reached. The old app is healthy, login returns 200, unauthenticated worker API 401, chain and submission ledger are valid, and the lease is ACTIVE_LEASE. The failed replacement is stopped with unless-stopped policy; no simultaneous database writer remains.

The relay environment also contains MC_RELAY_MIN_SUBMISSION_INTERVAL_MS=20000. Neither value was changed. Owner action: authorize restoring both PRIMARY minimums to 60000 ms, or explicitly resolve the conflict between preserving the live 20-second configuration and the controlling directive/source's 60-second gate. Work must not weaken the source gate to make rollout pass. After that decision continue this branch with successor/publication/discovery/preflight/capability/old-receipt acceptance. No ordinary provider work, Somatic, SECONDARY activation, or merge is authorized. This run's provider sends and nonce publications remain zero.

A separate pre-install check found the accepted four-file relay-lock repair had been dropped by a package replacement at 00:58 UTC, before this rollout. Its retained PRIMARY rollback copy matches all four exact source hashes at approved commit 5be4d444041c8e1681d56a5b7256a01b3090f690. The identical lock/state/CLI correction and regression tests are now composed into this branch, preserving the three rotation-discovery changes. No live relay files were installed in this blocked rollout. Restore that exact repair with the future relay deployment; do not overwrite it with an older full package.

Verification checkpoint: Mission Control 380/380, repository 299/299; production build/typecheck and deterministic repository audit passed. Combined rotation/lock-repair relay source passed 215/215 full tests, followed by 12/12 focused state/client tests including the additionally preserved CLI cleanup regression. Exact publisher bundle SHA-256 on PRIMARY: `5b7fa2ad34924fd928ae23092e503a1e76f0f9aae66022d49ef344876cc7eac5`. Bounded operator-script review caught a retained-container restart-policy hazard; an explicit unless-stopped prerequisite closed it before rollout. A new pre-stop pacing prerequisite prevents repeating the observed outage. No remaining blocking source-review finding; the live pacing decision remains owner-only.
