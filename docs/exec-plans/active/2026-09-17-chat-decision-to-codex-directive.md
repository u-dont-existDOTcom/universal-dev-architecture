# Canonical Chat decision to Codex directive publication

Status: VALIDATED REVERSIBLE CANDIDATE — REMOTE PUBLICATION PENDING

Assurance lane: iteration with a targeted authority/provenance hard gate.

## Owner outcome and remaining gap

`OPEN`. The reviewed downstream chain begins at a durable schema-v3 execution
directive. Natural operation still requires manual EventStore seeding because
the accepted canonical GitHub supervisory decision does not publish that
directive. This slice closes only that upstream connection.

Owner source: `docs/requirements/2026-09-17-chat-decision-to-codex-directive.owner-requirement.json`

Starting reviewed candidate: `b5e0fb5bb85904a6e31a7f51e803e2b22ec2128f`

Current-main-compatible base: `80aeef8ac138bfb2e9cf1809ffb6b0144da7be29`

Task branch: `task/mission-control-codex-directive-publication-20260917`

## Work preflight

- Residual class: `GENUINELY_DIFFICULT`.
- Reason: the change crosses canonical GitHub validation, event provenance,
  schema-v3 execution authority, relay discovery, and persisted admission.
- Owner-requested minimum: GPT-5.6 Sol Extra High; Fast disabled.
- Runtime model/effort readback is unavailable on this surface, so no model
  identity or effective effort will be fabricated.
- Direct owner-authorized repository maintenance uses the maintenance path;
  autonomous runtime admission remains part of the product acceptance seam.

## Smallest implementation

1. Add an optional complete bounded-execution block only to the current
   canonical decision schema and copy its canonical digest into the accepted
   GitHub receipt.
2. After all existing decision checks pass, atomically append a deterministic
   SYSTEM schema-v3 execution directive whose provenance names the exact
   accepted receipt event. Keep GitHub-attested content explicitly
   `UNVERIFIED`.
3. Make EventStore and admission recognize that validated server-side proof as
   an alternative authority path. Preserve the existing verified and
   owner-attested reasoning-message path unchanged.
4. Make relay discovery verify the exact receipt, SYSTEM producer, residue
   digest, payload bytes, and directive artifact before dispatch.
5. Cover idempotency, changed residue, malformed/untrusted input, WORKER minting,
   direct-source compatibility, local dispatch, and legacy browser fallback.
6. Run one direct deterministic canonical-receipt-to-RelayRuntime-to-fake-Codex
   acceptance, then affected tests, the complete relay package once,
   TypeScript, and JavaScript syntax checks.
7. Review the public diff, commit, push, and verify the remote exact commit.

## Non-satisfying proxies and boundaries

Manual reasoning-message/directive/profile/preflight seeding does not satisfy
the consumer seam. A raw GitHub comment, caller-supplied proof identity, or
fabricated provider-direct provenance does not satisfy authority.

Do not modify `main`, the reviewed mainline-port branch,
`state/CURRENT-STATE.md`, production, browser-fence/controller behavior, or
live configuration. Do not open a PR, merge, deploy, install, send provider
messages, or run a real model/API qualification.

## Candidate result

- The current route-v4 writer prompt can author one optional complete
  `bounded_execution` block; decisions without the block remain compatible.
- Existing GitHub receipt validation runs first. The authenticated receipt
  SYSTEM then atomically records the receipt, attestation, and exact derived
  schema-v3 directive.
- The directive proof binds the exact receipt event, canonical decision hash,
  residue hash, and derived payload. The durable source remains
  `GITHUB_SESSION_ATTESTED` / `UNVERIFIED`; admission trusts only the validated
  server proof.
- Exact duplicate ingestion is idempotent. Reusing the immutable comment
  identity with changed canonical bytes fails closed and requires a new
  authoritative decision.
- The direct consumer seam passed from canonical GitHub validation through
  `RelayRuntime.cycle`, actual admission, trusted setter evidence, persisted
  preflight, fake `CODEX_LOCAL`, `codex_execution_started`, and
  `execution_receipt_recorded`, with no manual execution-state seeding.
- Affected Mission Control checks passed: 170 tests. Complete relay checks
  passed: 232 tests plus JavaScript syntax. TypeScript passed. No live/model/API
  run, provider send, deployment, installation, main change, browser-fence
  release, or controller change occurred.
