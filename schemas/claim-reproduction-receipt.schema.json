{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://u-dont-exist.com/schemas/mission-control/claim-reproduction-receipt.schema.json",
  "title": "Subject-bound independent reproduction receipt",
  "type": "object",
  "additionalProperties": false,
  "required": ["$schema", "schemaVersion", "reproductionReceiptId", "claimRef", "subjectRef", "producerEvidenceRef", "independenceRegistryRef", "independenceRegistryDigest", "independenceAdmissionRef", "independenceAdmissionDigest", "reproducer", "independenceBasis", "methodRef", "methodDigest", "methodByteLength", "methodBytesDefinition", "commandOrProcedure", "resultValue", "resultDigest", "resultByteLength", "resultBytesDefinition", "matchState", "synthetic", "reproducedAt", "freshnessState", "promotesAuthority"],
  "properties": {
    "$schema": {"const": "../schemas/claim-reproduction-receipt.schema.json"},
    "schemaVersion": {"const": 1},
    "reproductionReceiptId": {"type": "string", "minLength": 1},
    "claimRef": {"$ref": "#/$defs/claimRef"},
    "subjectRef": {"type": "object", "minProperties": 2},
    "producerEvidenceRef": {"type": "string", "minLength": 1},
    "independenceRegistryRef": {"type": "string", "minLength": 1},
    "independenceRegistryDigest": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "independenceAdmissionRef": {"type": "string", "minLength": 1},
    "independenceAdmissionDigest": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "reproducer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["identityRef", "type", "trustDomain"],
      "properties": {
        "identityRef": {"type": "string", "minLength": 1},
        "type": {"const": "HUMAN_OR_INDEPENDENT_PROCESS"},
        "trustDomain": {"type": "string", "minLength": 1}
      }
    },
    "independenceBasis": {"type": "string", "minLength": 1},
    "methodRef": {"type": "string", "minLength": 1},
    "methodDigest": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "methodByteLength": {"type": "integer", "minimum": 0},
    "methodBytesDefinition": {"const": "exact method/procedure UTF-8 bytes"},
    "commandOrProcedure": {"type": "string", "minLength": 1},
    "resultValue": true,
    "resultDigest": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "resultByteLength": {"type": "integer", "minimum": 0},
    "resultBytesDefinition": {"const": "RFC8785_JCS resultValue UTF-8 bytes"},
    "matchState": {"enum": ["MATCH", "MISMATCH", "INCONCLUSIVE"]},
    "synthetic": {"type": "boolean"},
    "reproducedAt": {"type": "string", "format": "date-time", "pattern": "^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](\\.[0-9]+)?(Z|[+-]([01][0-9]|2[0-3]):[0-5][0-9])$"},
    "freshnessState": {"enum": ["CURRENT", "STALE"]},
    "promotesAuthority": {"const": false}
  },
  "$defs": {
    "claimRef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["claimId", "claimVersion", "claimDigest"],
      "properties": {
        "claimId": {"type": "string", "minLength": 1},
        "claimVersion": {"type": "integer", "minimum": 1},
        "claimDigest": {"type": "string", "pattern": "^[0-9a-f]{64}$"}
      }
    }
  }
}
