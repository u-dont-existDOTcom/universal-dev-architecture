# Mission Control production endpoint. HTTPS is required outside localhost.
MC_RELAY_MISSION_CONTROL_URL=https://mission-control-production-de75.up.railway.app

# Use a dedicated machine credential already registered in
# MISSION_CONTROL_INGEST_CREDENTIALS. V1 uses the existing read-only MCP fleet tool.
MC_RELAY_PRODUCER_ID=supervisor:chatgpt-relay-reader
MC_RELAY_TOKEN=REPLACE_WITH_AT_LEAST_32_RANDOM_CHARACTERS

# Exact chat locators are stored separately with owner-only permissions.
# MC_RELAY_CHATS_FILE=/home/USER/.config/mission-control-chatgpt-relay/chats.json

# Browser control is loopback-only. Chrome 136+ requires this dedicated non-default profile.
MC_RELAY_CDP_HOST=127.0.0.1
MC_RELAY_CDP_PORT=9222
# MC_RELAY_BROWSER_BIN=/usr/bin/brave-browser
# MC_RELAY_DISPLAY=:0

# Keep disabled through login and doctor checks. Set to 1 only for the bounded live trial.
MC_RELAY_SUBMIT_ENABLED=0
MC_RELAY_POLL_INTERVAL_MS=15000
MC_RELAY_RETRY_DELAY_MS=300000
MC_RELAY_MAX_HOT_TABS=3

# Conservative 16 GB VPS envelope. The systemd cgroup is the hard backstop.
MC_RELAY_MEMORY_SOFT_AVAILABLE_MB=4096
MC_RELAY_MEMORY_HARD_AVAILABLE_MB=2048
MC_RELAY_BROWSER_SOFT_RSS_MB=7168
MC_RELAY_BROWSER_HARD_RSS_MB=9216
MC_RELAY_SWAP_SOFT_USED_MB=512
MC_RELAY_SWAP_HARD_USED_MB=1536

MC_RELAY_HTTP_TIMEOUT_MS=30000
MC_RELAY_PAGE_READY_TIMEOUT_MS=90000
MC_RELAY_SUBMIT_TIMEOUT_MS=30000
